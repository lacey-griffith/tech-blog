<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>A place where nerds and notes combine.</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h1>
        <a href="/">a place to blog</a>
      </h1>
      {{#if loggedIn}}
      <h4>{{username}}</h4>
      {{/if}}
      <nav>
          {{#if loggedIn}}
          <a href='/dashboard'>dashboard</a>
          <button id='logout' class='btn-no-style'>logout</button>
          {{else}}
          <a href='/login'>login</a>
          {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      made with &hearts;
    </footer>
  </div>

{{#if loggedIn}}
<script src="/javascript/logout.js"></script>
{{/if}}
</body>

</html>